<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="SnowPal.Pages.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnowPal.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="ThisPage">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Text="SnowPal" Style="{StaticResource TitleTextStyle}" Grid.ColumnSpan="2"/>

        <!-- Drawing -->
        <Image x:ConnectionId='2' x:Name="Image" Grid.Row="1" Grid.Column="0" Style="{StaticResource ImageStyle}"                                                     />
        <!-- Guesses Left Display -->
        <StackPanel Grid.Row="1" Grid.Column="1">
            <StackPanel Style="{StaticResource HorizontalCenterStackPanel}">
                <TextBlock Text="Guesses Left: " Style="{StaticResource BaseTextBlock}"/>
                <TextBlock x:ConnectionId='11'                                                    Style="{StaticResource BaseTextBlock}"/>
            </StackPanel>
        </StackPanel>

        <!-- Alphabet Buttons -->
        <GridView x:ConnectionId='3' x:Name="AlphabetButtonsGridView" Grid.Row="1" Grid.Column="1" Style="{StaticResource GridViewStyle}" 
          ScrollViewer.HorizontalScrollMode="Enabled"
          ScrollViewer.HorizontalScrollBarVisibility="Auto"
          ScrollViewer.VerticalScrollMode="Disabled"
          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                  >
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="5"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate                              >
                    <Button x:ConnectionId='10'                                                                            Style="{StaticResource AlphabetButton}"                                      
                    Command="{Binding ViewModel.LetterGuessedCommand, ElementName=ThisPage}"/>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

        <!-- Word Display Area -->
        <TextBlock x:ConnectionId='4' x:Name="Word" Grid.Row="2" Grid.ColumnSpan="2"                                                    Style="{StaticResource LargerTextBlock}"/>

        <!-- Popup for message -->
        <Grid x:ConnectionId='5' x:Name="Output" Style="{StaticResource GridStyle}">
            <Popup x:ConnectionId='6' x:Name="StandardPopup" Style="{StaticResource PopupStyle}" IsLightDismissEnabled="False"                                                        >
                <Border Style="{StaticResource BorderStyle}" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}">
                    <StackPanel Style="{StaticResource VerticalCenterStackPanel}">
                        <TextBlock x:ConnectionId='7'                                                     Style="{StaticResource LargerTextBlock}" />
                        <TextBlock x:ConnectionId='8'                                                       Style="{StaticResource BaseTextBlock}"/>
                        <Button Content="Close" Command="{Binding ViewModel.ClosePopupClickedCommand, ElementName=ThisPage}" Style="{StaticResource PopupButton}"/>
                    </StackPanel>
                </Border>
            </Popup>
        </Grid>
    </Grid>
</Page>

