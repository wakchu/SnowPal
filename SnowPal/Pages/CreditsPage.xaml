<Page
    x:Class="SnowPal.Pages.CreditsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SnowPal.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource BackgroundBrush}">
        <StackPanel Style="{StaticResource VerticalCenterStackPanel}">

            <TextBlock Text="Credits" Style="{StaticResource TitleTextStyle}" Margin="0,0,0,60"/>

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <TextBlock Text="Created by: Bordoni Stefano" Style="{StaticResource BaseTextBlock}"/>
                <TextBlock Text="Thanks to the WinUI 101 tutorial" Style="{StaticResource BaseTextBlock}"/>
                <TextBlock Text="Powered by: WinUI &amp; .NET" Style="{StaticResource BaseTextBlock}"/>
            </StackPanel>

            <Button Content="Back" Style="{StaticResource MenuButton}" Click="Back_Click" Margin="0,60,0,0"/>
        </StackPanel>
    </Grid>
</Page>
